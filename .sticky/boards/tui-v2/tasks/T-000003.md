---
id: T-000003
uid: 7eb4fd59-ba91-4b3e-9adf-05a6f8f25220
title: 'Boards: List View + Sort/Filter UI (backed by ListOptions)'
status: done
priority: 2
tags:
    - tui
    - ux
    - v2
created: "2026-02-15"
depends_on: []
---
Goal: Provide a compact list view and user-driven sorting/filtering.

Scope:
- Implement L toggle between Kanban and List view.
- List columns use supported fields only: ID, Status, Priority, Title, Tags, Created, DependsOn.
- Add F filter UI (status/title/tags) and O sort UI (status/created/title/priority + desc).
- Keep current TUI default ordering (readiness/priority/title) as an option or fallback.

Implemented (2026-02-17):
- Added board list-view data pipeline backed by board.ListOptions, including status/title/tags filters and status/created/title/priority sorting.
- Added list-view fallback sort mode readiness as default behavior so existing readiness-first ordering remains available.
- Added task-pane keyboard actions for list controls:
  - F opens Board Filters menu (status, title, tags, clear)
  - O opens Board Sort menu (readiness/status/created/title/priority + asc/desc toggle)
  - L toggles Kanban and List view.
- Added board filter input modal and sort/filter selection modals using the same inline modal pattern as the rest of the TUI.
- Implemented compact board list table rendering with supported fields: ID, Status, Pri, Title, Tags, Created, Deps.
- Added filter/sort summary chips in list view header and ensured Enter still opens task detail from selected list row.
- Ensured list navigation uses filtered/sorted row order and preserves task selection mapping back to board columns.

Acceptance:
- List view renders correctly and opens task detail.
- Filtering and sorting work and are persisted while staying in the board.

Validation (2026-02-17):
- go test ./internal/tui
- go test ./...
