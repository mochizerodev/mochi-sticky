---
id: T-000004
uid: 09ee14fe-f416-4d6b-9d64-22e865f137e1
title: Unified Modals + Toast Feedback
status: done
priority: 2
tags:
    - tui
    - ux
    - v2
created: "2026-02-15"
depends_on: []
---
Goal: Replace full confirmation pages and improve feedback consistency.

Scope:
- Implement reusable modal overlay for confirm/pickers.
- Implement toast queue (success/error/info) displayed in footer.
- Wire archive/delete/restore flows to modals + toasts.

Implemented (2026-02-17):
- Kept confirmations/pickers on the shared centered modal overlay path (no full-screen confirmation pages in common actions).
- Updated confirmation UX to use Enter/Esc as primary actions (with y/n still accepted), matching the board spec.
- Added destructive confirmation styling in confirm modal copy (delete actions shown as danger intent).
- Added a unified toast system in the TUI model with level support (success/error/info) and small queue retention.
- Added a dedicated toast bar rendered above the footer/status row for consistent inline feedback.
- Wired mutation feedback to toasts across key flows:
  - board use/create/rename/archive/delete,
  - task create/update/archive/delete/restore,
  - task status moves,
  - ADR status updates and ADR deletion,
  - wiki export status,
  - non-canceled errors emit error toast.
- Cleared pending success toast intents when commands fail so stale success messages are not shown.

Acceptance:
- No full-screen confirmation pages for common actions.
- All core mutations emit a toast; destructive actions remain clearly confirmed.

Validation (2026-02-17):
- go test ./internal/tui
- go test ./...
